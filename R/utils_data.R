#' data 
#'
#' @description A utils function
#'
#' @return The return value, if any, from executing the utility.
#'
#' @noRd
loaded_dataset <- reactiveValues(data = NULL)
results <- reactiveValues(results = NULL)
tab <- reactiveValues(tab = "epi_panel")